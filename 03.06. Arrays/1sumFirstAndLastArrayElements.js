function solve(elements){
    let secondNumber = Number(elements[elements.length-1]);
    let firstNumber = Number(elements[0])
    console.log(firstNumber+secondNumber)
}

solve([ '10', '17', '22', '33' ]
)